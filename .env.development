# Vite carrega .env.development DEPOIS de .env.local e SOBRESCREVE as mesmas chaves.
# Por isso NÃO coloque VITE_FIREBASE_* aqui — isso quebrava o login quando você usava .env.local.
#
# Projeto na nuvem: use só .env.local (copie de env.local.sample).
# Emuladores: copie env.emulator.sample para .env.development.local (git ignora *.local).
